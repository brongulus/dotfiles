#!/usr/bin/env bash
# emacsclient -a "" --eval "(run-with-timer 0.2 nil 'org-capture)"
# kitty tmux a -t scratch \; new-window -n note \; send-keys emacs\ -nw\ -f\ remember C-m;
emacsclient -a "" --eval "(remember)"
